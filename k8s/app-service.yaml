apiVersion: v1
kind: Service
metadata:
  name: oficina-app-service
  namespace: oficina-mecanica
spec:
  type: LoadBalancer
  selector:
    app: oficina-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
